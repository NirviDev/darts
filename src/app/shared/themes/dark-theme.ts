import { Theme } from './symbols';

export const darkTheme: Theme = {
  name: 'dark',
  properties: {
    '--nav': 'rgb(0, 0, 0)',
    '--score-panel': 'rgb(200, 200, 200)',
    '--text': 'rgb(255, 255, 255)',
  }
}
