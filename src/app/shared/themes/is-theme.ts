import { Theme } from './symbols';

export const isTheme: Theme = {
  name: 'is',
  properties: {
    '--nav-bg': 'rgb(36, 209, 163)',
    '--score-panel': 'rgba(44, 178, 188, 0.8)',
    '--text': 'rgb(255, 255, 255)',
  }
}
